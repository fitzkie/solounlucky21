/** @type {import('next').NextConfig} */
const nextConfig = {
  // Revalidate API routes every 30 seconds by default
  // Individual routes can override with their own revalidate
}

module.exports = nextConfig
