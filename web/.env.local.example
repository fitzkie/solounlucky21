# Copy to .env.local for local dev, set in Railway dashboard for production

# VPS PostgreSQL — read-only web user
# Format: postgresql://USER:PASSWORD@HOST:PORT/DBNAME?sslmode=require
DATABASE_URL=postgresql://unlucky21_web:u21webread2026@108.61.202.106:5432/unlucky21?sslmode=require

# Set to 'false' only for local dev without SSL (never in production)
DATABASE_SSL=true
